import { CapacitorConfig }
from "@capacitor/cli";

const config: CapacitorConfig = {

  appId: "com.holospin.x",

  appName: "HOLOSPIN X",

  webDir: "dist",

  bundledWebRuntime: false

};

export default config;
